sequenceDiagram
    participant User
    participant Browser
    participant Server

    User ->> Browser: Enters URL
    Browser ->> Server: Requests SPA
    Server -->> Browser: Sends SPA
    Browser -->> User: Renders and displays SPA
